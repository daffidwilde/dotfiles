- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc

- shell:
    - [chsh $(which zsh), Making ZSH the default shell.]
